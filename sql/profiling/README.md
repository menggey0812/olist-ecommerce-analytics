# Profiling Queries (Exploratory)

This folder is intentionally non-executable.

During initial dataset onboarding, exploratory SQL queries were used to profile row counts, key integrity, null distributions, and value domains.

These queries were run interactively and informed:
- staging transformations
- validation rules
- documented assumptions (see Phase 5)

They are not part of the automated pipeline by design.